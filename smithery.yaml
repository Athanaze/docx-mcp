# Smithery configuration file: https://smithery.ai/docs/build/project-config

startCommand:
  type: http
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    description: No configuration options required
  commandFunction:
    # Start MCP server in streamable HTTP mode.
    |-
    (config) => ({command:'word_mcp_server', args:[], env:{MCP_TRANSPORT:'streamable-http'}})
  exampleConfig: {}
